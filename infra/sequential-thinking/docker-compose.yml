services:
  sequential-thinking-wrapper:
    build: .
    container_name: ddg-sequential-thinking-wrapper
    environment:
      SEQUENTIAL_WRAPPER_HOST: 0.0.0.0
      SEQUENTIAL_WRAPPER_PORT: 38001
      SEQUENTIAL_WRAPPER_PATH: /mcp
    ports:
      - "38001:38001"
    restart: unless-stopped
